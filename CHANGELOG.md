# Changelog

All notable changes to **medagent-core** are documented here.
Follows [Keep a Changelog](https://keepachangelog.com/en/1.0.0/).

## [Unreleased]

### Added
- Statin + fibrate myopathy/rhabdomyolysis checker (`safety/statin_fibrate_checker.py`) that flags simvastatin/Zocor, lovastatin/Mevacor/Altoprev, atorvastatin/Lipitor, rosuvastatin/Crestor, pravastatin/Pravachol, fluvastatin/Lescol, or pitavastatin/Livalo co-prescribed with gemfibrozil/Lopid (`CRITICAL`) or fenofibrate/Tricor/Lofibra / fenofibric acid/Trilipix (`HIGH`) as advisory `StatinFibrateRisk` records. Concurrent statin–fibrate therapy intensifies myopathy and rhabdomyolysis risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This control is distinct from statin CYP3A4 and statin macrolide checkers. Exported from `medagent.safety`. Documented as Safety Control #98 (SAFETY.md §3.98); see `docs/guides/STATIN_FIBRATE_GUIDE.md` and `assets/statin_fibrate_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Colchicine + strong CYP3A4/P-gp inhibitor toxicity checker (`safety/colchicine_cyp3a4_checker.py`) that flags colchicine/Colcrys/Mitigare/Gloperba co-prescribed with clarithromycin, ketoconazole, itraconazole, ritonavir, cyclosporine/ciclosporin, cobicistat, or posaconazole as advisory `ColchicineCyp3a4Risk` records with `CRITICAL` severity. Strong CYP3A4 and/or P-gp inhibition can markedly increase colchicine exposure and cause severe or fatal toxicity (FDA boxed-warning territory). Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This control is distinct from the fentanyl CYP3A4 exposure checker. Exported from `medagent.safety`. Documented as Safety Control #95 (SAFETY.md §3.95); see `docs/guides/COLCHICINE_CYP3A4_GUIDE.md` and `assets/colchicine_cyp3a4_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- DOAC + strong inducer thrombosis-risk checker (`safety/doac_inducer_checker.py`) that flags apixaban/Eliquis, rivaroxaban/Xarelto, edoxaban/Savaysa, or dabigatran/Pradaxa co-prescribed with rifampin/rifampicin (`CRITICAL`) or carbamazepine/Tegretol, phenytoin/Dilantin, or St John's wort / hypericum (`HIGH`) as advisory `DoacInducerRisk` records. Strong CYP3A4/P-gp induction can reduce DOAC exposure and increase thrombosis risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This control is distinct from warfarin interaction checkers and DOAC bleeding intensifiers. Exported from `medagent.safety`. Documented as Safety Control #97 (SAFETY.md §3.97); see `docs/guides/DOAC_INDUCER_GUIDE.md` and `assets/doac_inducer_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Clozapine + strong CYP1A2 inhibitor exposure checker (`safety/clozapine_cyp1a2_checker.py`) that flags clozapine/Clozaril/FazaClo/Versacloz co-prescribed with fluvoxamine/Luvox (`CRITICAL`) or ciprofloxacin/Cipro (`HIGH`) as advisory `ClozapineCyp1a2Risk` records. Strong CYP1A2 inhibition can markedly elevate clozapine serum levels and increase seizure and myocarditis risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This control is distinct from the clozapine ANC monitoring checker. Exported from `medagent.safety`. Documented as Safety Control #96 (SAFETY.md §3.96); see `docs/guides/CLOZAPINE_CYP1A2_GUIDE.md` and `assets/clozapine_cyp1a2_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Fentanyl + CYP3A4 inhibitor exposure checker (`safety/fentanyl_cyp3a4_checker.py`) that flags fentanyl/Duragesic/Abstral/Fentora/Actiq co-prescribed with strong CYP3A4 inhibitors (ketoconazole, itraconazole, ritonavir, clarithromycin, nefazodone — `CRITICAL`) or moderate inhibitors (erythromycin, fluconazole, diltiazem, verapamil — `HIGH`) as advisory `FentanylCyp3a4Risk` records. CYP3A4 inhibition raises fentanyl exposure and respiratory-depression risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This control is distinct from opioid + benzodiazepine and general opioid MED checkers. Exported from `medagent.safety`. Documented as Safety Control #94 (SAFETY.md §3.94); see `docs/guides/FENTANYL_CYP3A4_GUIDE.md` and `assets/fentanyl_cyp3a4_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Lamotrigine + valproate SJS/TEN risk checker (`safety/lamotrigine_valproate_checker.py`) that flags lamotrigine/Lamictal co-prescribed with valproate, valproic acid, divalproex, or Depakote as advisory `LamotrigineValproateRisk` records with `CRITICAL` severity. Valproate inhibits lamotrigine metabolism, raising exposure and serious cutaneous-reaction (SJS/TEN) risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This control is distinct from the valproate × carbapenem level-drop checker. Exported from `medagent.safety`. Documented as Safety Control #93 (SAFETY.md §3.93); see `docs/guides/LAMOTRIGINE_VALPROATE_GUIDE.md` and `assets/lamotrigine_valproate_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Valproate + carbapenem precipitous level-drop checker (`safety/valproate_carbapenem_checker.py`) that flags valproate/valproic acid/divalproex/Depakote/Depakene co-prescribed with meropenem, ertapenem, imipenem, doripenem, or carbapenem as advisory `ValproateCarbapenemRisk` records with `CRITICAL` severity. Carbapenems can cause a precipitous drop in serum valproate levels, increasing breakthrough seizure risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This control is distinct from general AED interaction screens. Exported from `medagent.safety`. Documented as Safety Control #92 (SAFETY.md §3.92); see `docs/guides/VALPROATE_CARBAPENEM_GUIDE.md` and `assets/valproate_carbapenem_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Methadone + QT-prolonging drug intensification checker (`safety/methadone_qt_checker.py`) that flags methadone/Dolophine/Methadose co-prescribed with haloperidol, ziprasidone, or citalopram (`CRITICAL`) or ondansetron, azithromycin, or escitalopram (`HIGH`) as advisory `MethadoneQtRisk` records. Methadone's baseline QT-prolonging effect can be intensified by a second QT-prolonging agent, increasing torsades de pointes risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic and severity-sorted. This methadone-focused intensifier is distinct from the general multi-drug QT-prolongation screen (`safety/qt_prolongation_checker.py`). Exported from `medagent.safety`. Documented as Safety Control #91 (SAFETY.md §3.91); see `docs/guides/METHADONE_QT_GUIDE.md` and `assets/methadone_qt_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Metformin + iodinated contrast media lactic acidosis checker (`safety/metformin_contrast_checker.py`) that flags metformin/Glucophage/Fortamet/Glumetza/Riomet co-prescribed with contrast, contrast-media, iohexol, iodixanol, or iopamidol as advisory `MetforminContrastRisk` records with `HIGH` severity. Iodinated contrast can transiently impair renal clearance of metformin, and accumulation can precipitate life-threatening lactic acidosis. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from general metformin renal-dose checking. Exported from `medagent.safety`. Documented as Safety Control #89 (SAFETY.md §3.89); see `docs/guides/METFORMIN_CONTRAST_GUIDE.md` and `assets/metformin_contrast_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Isotretinoin + tetracycline-class antibiotic pseudotumor cerebri checker (`safety/isotretinoin_tetracycline_checker.py`) that flags isotretinoin/Accutane/Absorica/Claravis/Myorisan/Zenatane co-prescribed with tetracycline, doxycycline, or minocycline as advisory `IsotretinoinTetracyclineRisk` records with `CRITICAL` severity. Both agents are independently associated with idiopathic intracranial hypertension, and co-prescription can precipitate pseudotumor cerebri with irreversible vision loss. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #90 (SAFETY.md §3.90); see `docs/guides/ISOTRETINOIN_TETRACYCLINE_GUIDE.md` and `assets/isotretinoin_tetracycline_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- ACEI/ARB + potassium supplement hyperkalemia checker (`safety/acei_potassium_checker.py`) that flags supported ACE inhibitors or ARBs co-prescribed with potassium, KCl, Klor-Con, or potassium chloride as advisory `AceiPotassiumRisk` records with `HIGH` severity. Concurrent exogenous potassium increases hyperkalemia risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from ACEI/ARB + potassium-sparing diuretic (#3.60) and ACEI/ARB + trimethoprim (#3.63) checks. Exported from `medagent.safety`. Documented as Safety Control #88 (SAFETY.md §3.88); see `docs/guides/ACEI_POTASSIUM_GUIDE.md` and `assets/acei_potassium_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Codeine + CYP2D6 inhibitor analgesia checker (`safety/codeine_cyp2d6_checker.py`) that flags codeine/Tylenol-with-codeine co-prescribed with fluoxetine, paroxetine, bupropion, quinidine, or terbinafine as advisory `CodeineCyp2d6Risk` records with `HIGH` severity. Strong CYP2D6 inhibition can reduce codeine bioactivation to morphine, blunt analgesia, and alter exposure. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from opioid + benzodiazepine, opioid MED, and tramadol interaction checks. Exported from `medagent.safety`. Documented as Safety Control #87 (SAFETY.md §3.87); see `docs/guides/CODEINE_CYP2D6_GUIDE.md` and `assets/codeine_cyp2d6_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Allopurinol + azathioprine/6-MP toxicity checker (`safety/allopurinol_azathioprine_checker.py`) that flags allopurinol/Zyloprim co-prescribed with azathioprine/Imuran/mercaptopurine/6-MP/Purinethol as advisory `AllopurinolAzathioprineRisk` records with `CRITICAL` severity. Xanthine oxidase inhibition markedly increases thiopurine exposure and can cause severe myelosuppression. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #86 (SAFETY.md §3.86); see `docs/guides/ALLOPURINOL_AZATHIOPRINE_GUIDE.md` and `assets/allopurinol_azathioprine_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Sildenafil + nitrate critical-hypotension checker (`safety/sildenafil_nitrate_checker.py`) that flags sildenafil/Viagra/Revatio co-prescribed with nitroglycerin/isosorbide/Imdur/Monoket as advisory `SildenafilNitrateRisk` records with `CRITICAL` severity. PDE-5 inhibition amplifies nitrate-mediated cyclic-GMP vasodilation and can cause profound, life-threatening hypotension. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #85 (SAFETY.md §3.85); see `docs/guides/SILDENAFIL_NITRATE_GUIDE.md` and `assets/sildenafil_nitrate_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Methotrexate + penicillin-class antibiotic toxicity checker (`safety/mtx_penicillin_checker.py`) that flags methotrexate/MTX co-prescribed with penicillin, penicillin-V, Pen-VK, amoxicillin, or ampicillin as advisory `MtxPenicillinRisk` records with `HIGH` severity. Penicillin-class antibiotics can reduce renal methotrexate clearance and increase exposure and toxicity risk. Whole-token and whole-alias matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from methotrexate + NSAID and methotrexate + TMP-SMX checks. Exported from `medagent.safety`. Documented as Safety Control #84 (SAFETY.md §3.84); see `docs/guides/MTX_PENICILLIN_GUIDE.md` and `assets/mtx_penicillin_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Tramadol + bupropion seizure-risk checker (`safety/tramadol_bupropion_checker.py`) that flags tramadol/Ultram co-prescribed with bupropion/Wellbutrin/Zyban as advisory `TramadolBupropionRisk` records with `HIGH` severity. Both agents lower the seizure threshold, so concurrent use can compound seizure risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from tramadol + SSRI/SNRI screening. Exported from `medagent.safety`. Documented as Safety Control #83 (SAFETY.md §3.83); see `docs/guides/TRAMADOL_BUPROPION_GUIDE.md` and `assets/tramadol_bupropion_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Colchicine + strong CYP3A4 inhibitor checker (`safety/colchicine_cyp3a4_checker.py`) that flags colchicine/Colcrys/Mitigare/Gloperba co-prescribed with clarithromycin, ketoconazole, itraconazole, or ritonavir as advisory `ColchicineCyp3a4Risk` records with `CRITICAL` severity. Strong CYP3A4 inhibition can markedly increase colchicine exposure and cause severe or fatal toxicity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #81 (SAFETY.md §3.81); see `docs/guides/COLCHICINE_CYP3A4_GUIDE.md` and `assets/colchicine_cyp3a4_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Lithium + thiazide diuretic checker (`safety/lithium_thiazide_checker.py`) that flags lithium/Lithobid/Eskalith co-prescribed with hydrochlorothiazide/HCTZ, chlorthalidone, or indapamide as advisory `LithiumThiazideRisk` records with `HIGH` severity. Thiazide-related sodium loss may reduce renal lithium clearance, raise serum concentrations, and cause toxicity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from lithium + NSAID and lithium + ACEI/ARB checks. Exported from `medagent.safety`. Documented as Safety Control #82 (SAFETY.md §3.82); see `docs/guides/LITHIUM_THIAZIDE_GUIDE.md` and `assets/lithium_thiazide_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Warfarin + metronidazole/tinidazole interaction checker (`safety/warfarin_metronidazole_checker.py`) that flags warfarin/Coumadin/Jantoven co-prescribed with metronidazole or tinidazole as advisory `WarfarinMetronidazoleRisk` records with `HIGH` severity. CYP2C9 inhibition may reduce warfarin clearance, elevate INR, and increase bleeding risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #80 (SAFETY.md §3.80); see `docs/guides/WARFARIN_METRONIDAZOLE_GUIDE.md` and `assets/warfarin_metronidazole_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Carbamazepine + CYP3A4-inhibiting macrolide checker (`safety/carbamazepine_macrolide_checker.py`) that flags carbamazepine/Tegretol/Carbatrol/Equetro co-prescribed with clarithromycin or erythromycin as advisory `CarbamazepineMacrolideRisk` records with `HIGH` severity. CYP3A4 inhibition may reduce carbamazepine metabolism, raise serum concentrations, and increase neurologic and other dose-related toxicity. Azithromycin is intentionally excluded. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #79 (SAFETY.md §3.79); see `docs/guides/CARBAMAZEPINE_MACROLIDE_GUIDE.md` and `assets/carbamazepine_macrolide_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Amiodarone + digoxin P-glycoprotein interaction checker (`safety/amiodarone_digoxin_checker.py`) that flags amiodarone/Cordarone/Pacerone co-prescribed with digoxin/Lanoxin as advisory `AmiodaroneDigoxinRisk` records with `HIGH` severity. Amiodarone inhibits P-glycoprotein and reduces digoxin clearance, which can substantially raise serum digoxin concentrations and toxicity risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This named amiodarone-first record overlaps the older digoxin + amiodarone monitor but is distinct from digoxin + verapamil screening. Exported from `medagent.safety`. Documented as Safety Control #78 (SAFETY.md §3.78); see `docs/guides/AMIODARONE_DIGOXIN_GUIDE.md` and `assets/amiodarone_digoxin_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Theophylline + CYP1A2-inhibiting quinolone toxicity checker (`safety/theophylline_cipro_checker.py`) that flags theophylline/aminophylline/Uniphyl/TheoChron co-prescribed with ciprofloxacin/Cipro (`HIGH`) or enoxacin (`CRITICAL`) as advisory `TheophyllineCiproRisk` records. CYP1A2 inhibition may reduce theophylline clearance, raise serum concentrations, and increase seizure and serious-arrhythmia risk. Other fluoroquinolones remain outside this focused panel. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #77 (SAFETY.md §3.77); see `docs/guides/THEOPHYLLINE_CIPRO_GUIDE.md` and `assets/theophylline_cipro_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- Lithium + ACE inhibitor/ARB toxicity checker (`safety/lithium_acei_checker.py`) that flags lithium/Lithobid/Eskalith co-prescribed with supported ACE inhibitors or ARBs as advisory `LithiumAceiRisk` records with `HIGH` severity. ACE inhibitors and ARBs may reduce renal lithium clearance, increase serum lithium concentrations, and cause toxicity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from ACEI/ARB duplication, ACEI/ARB + trimethoprim, and lithium + NSAID checks. Exported from `medagent.safety`. Documented as Safety Control #76 (SAFETY.md §3.76); see `docs/guides/LITHIUM_ACEI_GUIDE.md` and `assets/lithium_acei_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.

- PPI + methotrexate toxicity checker (`safety/ppi_mtx_checker.py`) that flags methotrexate co-prescribed with omeprazole, esomeprazole, pantoprazole, lansoprazole, or rabeprazole as advisory `PpiMtxRisk` records with `HIGH` severity. PPIs may reduce methotrexate clearance and increase exposure and toxicity risk. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from methotrexate + NSAID and clopidogrel + PPI checks. Exported from `medagent.safety`. Documented as Safety Control #74 (SAFETY.md §3.74); see `docs/guides/PPI_MTX_GUIDE.md` and `assets/ppi_mtx_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Linezolid + SSRI/SNRI serotonin-syndrome checker (`safety/linezolid_ssri_checker.py`) that flags linezolid co-prescribed with sertraline, fluoxetine, paroxetine, citalopram, escitalopram, venlafaxine, or duloxetine as advisory `LinezolidSsriRisk` records with `CRITICAL` severity. Linezolid's reversible MAOI-like activity can combine with serotonergic antidepressants to precipitate potentially life-threatening serotonin syndrome. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. This control is distinct from tramadol + SSRI/SNRI and SSRI/SNRI + triptan checks. Exported from `medagent.safety`. Documented as Safety Control #75 (SAFETY.md §3.75); see `docs/guides/LINEZOLID_SSRI_GUIDE.md` and `assets/linezolid_ssri_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Warfarin + systemic azole antifungal interaction checker (`safety/warfarin_azole_checker.py`) that flags warfarin/Coumadin co-prescribed with fluconazole/voriconazole (`CRITICAL`) or ketoconazole/itraconazole (`HIGH`) as advisory `WarfarinAzoleRisk` records. Systemic azole inhibition of CYP2C9 and other warfarin-metabolizing CYP pathways can elevate INR and bleeding risk; topical clotrimazole is intentionally excluded. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #68 (SAFETY.md §3.68); see also `docs/guides/WARFARIN_AZOLE_GUIDE.md` and `assets/warfarin_azole_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- DOAC + NSAID bleeding intensifier checker (`safety/doac_nsaid_checker.py`) that flags apixaban/rivaroxaban/edoxaban/dabigatran co-prescribed with NSAID partners (ibuprofen, naproxen, diclofenac, ketorolac, meloxicam, celecoxib) as advisory `DoacNsaidRisk` records with `HIGH` severity for typical NSAIDs and `CRITICAL` for ketorolac. Concurrent DOAC anticoagulation with an NSAID intensifies major bleeding risk via anticoagulation plus GI mucosal injury and platelet dysfunction. Aspirin is intentionally excluded because DOAC + antiplatelet screening is a separate control. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #71 (SAFETY.md §3.71); see also `docs/guides/DOAC_NSAID_GUIDE.md` and `assets/doac_nsaid_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Safety #70:  — ACEI + sacubitril/Entresto duplication risk. See .
- Safety 3.69 Methotrexate + NSAID Toxicity:  — Methotrexate + NSAID toxicity. See .
- Warfarin + systemic azole antifungal interaction checker (`safety/warfarin_azole_checker.py`) that flags warfarin/Coumadin co-prescribed with fluconazole/voriconazole (`CRITICAL`) or ketoconazole/itraconazole (`HIGH`) as advisory `WarfarinAzoleRisk` records. Systemic azole inhibition of CYP2C9 and other warfarin-metabolizing CYP pathways can elevate INR and bleeding risk; topical clotrimazole is intentionally excluded. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #68 (SAFETY.md §3.68); see also `docs/guides/WARFARIN_AZOLE_GUIDE.md` and `assets/warfarin_azole_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Statin + macrolide CYP3A4 interaction checker (`safety/statin_macrolide_checker.py`) that flags simvastatin/lovastatin (`CRITICAL`) or atorvastatin (`HIGH`) co-prescribed with clarithromycin/erythromycin as advisory `StatinMacrolideRisk` records. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is a focused macrolide pair distinct from the broader statin + strong CYP3A4 inhibitor panel. Exported from `medagent.safety`. Documented as Safety Control #67 (SAFETY.md §3.67); see also `docs/guides/STATIN_MACROLIDE_GUIDE.md` and `assets/statin_macrolide_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Digoxin + verapamil toxicity checker (`safety/digoxin_verapamil_checker.py`) that flags digoxin/lanoxin co-prescribed with verapamil/calan/isoptin/verelan as advisory `DigoxinVerapamilRisk` records with `HIGH` severity (P-glycoprotein inhibition / reduced digoxin clearance). Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is distinct from digoxin + amiodarone level monitoring and macrolide + digoxin P-gp checks. Exported from `medagent.safety`. Documented as Safety Control #66 (SAFETY.md §3.66); see also `docs/guides/DIGOXIN_VERAPAMIL_GUIDE.md` and `assets/digoxin_verapamil_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Fluoroquinolone + corticosteroid tendon-risk checker (`safety/fluoroquinolone_corticosteroid_checker.py`) that flags ciprofloxacin/levofloxacin/moxifloxacin/ofloxacin co-prescribed with systemic corticosteroids (prednisone/prednisolone/methylprednisolone/dexamethasone/hydrocortisone/betamethasone) as advisory `FluoroquinoloneCorticosteroidRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is distinct from fluoroquinolone + NSAID CNS/seizure and fluoroquinolone + warfarin INR checks. Exported from `medagent.safety`. Documented as Safety Control #65 (SAFETY.md §3.65); see also `docs/guides/FLUOROQUINOLONE_CORTICOSTEROID_GUIDE.md` and `assets/fluoroquinolone_corticosteroid_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- SSRI/SNRI + triptan serotonin-syndrome pair checker (`safety/ssri_triptan_checker.py`) that flags SSRIs/SNRIs co-prescribed with triptans as advisory `SsriTriptanRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is distinct from the broader serotonin syndrome panel and NSAID + SSRI/SNRI bleeding checks. Exported from `medagent.safety`. Documented as Safety Control #64 (SAFETY.md §3.64); see also `docs/guides/SSRI_TRIPTAN_GUIDE.md` and `assets/ssri_triptan_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- ACEI/ARB + trimethoprim / TMP-SMX hyperkalemia checker (`safety/acei_trimethoprim_checker.py`) that flags ACE inhibitors or ARBs co-prescribed with trimethoprim/bactrim/septra/cotrimoxazole as advisory `AceiTrimethoprimRisk` records with `HIGH` (trimethoprim) or `CRITICAL` (TMP-SMX products) severity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is distinct from ACEI/ARB + potassium-sparing hyperkalemia and methotrexate + TMP-SMX myelosuppression checks. Exported from `medagent.safety`. Documented as Safety Control #63 (SAFETY.md §3.63); see also `docs/guides/ACEI_TRIMETHOPRIM_GUIDE.md` and `assets/acei_trimethoprim_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Fluoroquinolone + NSAID CNS/seizure-risk checker (`safety/fluoroquinolone_nsaid_checker.py`) that flags ciprofloxacin/levofloxacin/moxifloxacin/ofloxacin co-prescribed with NSAIDs as advisory `FluoroquinoloneNsaidRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is distinct from fluoroquinolone + warfarin INR and warfarin + NSAID bleeding checks. Exported from `medagent.safety`. Documented as Safety Control #62 (SAFETY.md §3.62); see also `docs/guides/FLUOROQUINOLONE_NSAID_GUIDE.md` and `assets/fluoroquinolone_nsaid_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- NSAID + SSRI/SNRI bleeding-intensifier checker (`safety/nsaid_ssri_checker.py`) that flags NSAIDs co-prescribed with SSRIs/SNRIs as advisory `NsaidSsriBleedRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is distinct from warfarin + NSAID bleeding and tramadol + SSRI/SNRI serotonin/seizure checks. Exported from `medagent.safety`. Documented as Safety Control #61 (SAFETY.md §3.61); see also `docs/guides/NSAID_SSRI_GUIDE.md` and `assets/nsaid_ssri_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- ACEI/ARB + potassium-sparing hyperkalemia checker (`safety/acei_ksparing_checker.py`) that flags ACE inhibitors or ARBs co-prescribed with spironolactone/eplerenone/amiloride/triamterene as advisory `AceiKsparingRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. The control is distinct from ACEI + ARB dual-blockade duplication. Exported from `medagent.safety`. Documented as Safety Control #60 (SAFETY.md §3.60); see also `docs/guides/ACEI_KSPARING_GUIDE.md` and `assets/acei_ksparing_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Fluoroquinolone + warfarin INR/bleeding interaction checker (`safety/fluoroquinolone_warfarin_checker.py`) that flags ciprofloxacin/levofloxacin/moxifloxacin/ofloxacin co-prescribed with warfarin/coumadin/jantoven as advisory `FluoroquinoloneWarfarinRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives, canonical pairs are de-duplicated, and findings are deterministic. Exported from `medagent.safety`. Documented as Safety Control #59 (SAFETY.md §3.59); see also `docs/guides/FLUOROQUINOLONE_WARFARIN_GUIDE.md` and `assets/fluoroquinolone_warfarin_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Amiodarone + warfarin INR interaction checker (`safety/amio_warfarin_checker.py`) that flags amiodarone/cordarone/pacerone co-prescribed with warfarin/coumadin/jantoven as advisory `AmioWarfarinRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #58 (SAFETY.md §3.58); see also `docs/guides/AMIO_WARFARIN_GUIDE.md` and `assets/amio_warfarin_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Methotrexate + TMP-SMX toxicity interaction checker (`safety/mtx_tmpsmx_checker.py`) that flags methotrexate co-prescribed with TMP-SMX panel partners (trimethoprim, sulfamethoxazole, bactrim, septra, cotrimoxazole) as advisory `MtxTmpsmxRisk` records with `CRITICAL` severity. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #56 (SAFETY.md §3.56); see also `docs/guides/MTX_TMPSMX_GUIDE.md` and `assets/mtx_tmpsmx_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- DOAC + antiplatelet bleed intensifier checker (`safety/doac_antiplatelet_checker.py`) that flags apixaban/rivaroxaban/edoxaban/dabigatran co-prescribed with antiplatelet partners (aspirin, clopidogrel, prasugrel, ticagrelor) as advisory `DoacAntiplateletRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #57 (SAFETY.md §3.57); see also `docs/guides/DOAC_ANTIPLATELET_GUIDE.md` and `assets/doac_antiplatelet_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Lithium + NSAID toxicity interaction checker (`safety/lithium_nsaid_checker.py`) that flags lithium/lithobid/eskalith co-prescribed with NSAID partners (ibuprofen, naproxen, diclofenac, indomethacin, ketorolac, meloxicam, piroxicam, celecoxib) as advisory `LithiumNsaidRisk` records with `HIGH` severity. Acetaminophen/paracetamol are excluded because they are not NSAIDs. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #55 (SAFETY.md §3.55); see also `docs/guides/LITHIUM_NSAID_GUIDE.md` and `assets/lithium_nsaid_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Macrolide + digoxin P-gp interaction checker (`safety/macrolide_digoxin_checker.py`) that flags digoxin/lanoxin co-prescribed with clarithromycin/erythromycin as advisory `MacrolideDigoxinRisk` records with `HIGH` severity (azithromycin excluded as a weaker P-gp inhibitor). Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #54 (SAFETY.md §3.54); see also `docs/guides/MACROLIDE_DIGOXIN_GUIDE.md` and `assets/macrolide_digoxin_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- SGLT2 + loop diuretic volume-depletion checker (`safety/sglt2_loop_checker.py`) that flags empagliflozin/dapagliflozin/canagliflozin/ertugliflozin co-prescribed with furosemide/bumetanide/torsemide/ethacrynic as advisory `Sglt2LoopRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #53 (SAFETY.md §3.53); see also `docs/guides/SGLT2_LOOP_GUIDE.md` and `assets/sglt2_loop_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Clozapine ANC monitoring checker (`safety/clozapine_anc_checker.py`) that flags clozapine/Clozaril/FazaClo therapy as advisory `ClozapineAncRisk` records with `CRITICAL` severity — reminding clinicians to confirm absolute neutrophil count (ANC) / neutrophil monitoring given agranulocytosis risk. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #52 (SAFETY.md §3.52); see also `docs/guides/CLOZAPINE_ANC_GUIDE.md` and `assets/clozapine_anc_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Tramadol + SSRI/SNRI seizure/serotonin dual-risk checker (`safety/tramadol_ssri_checker.py`) that flags tramadol/ultram co-prescribed with SSRI/SNRI partners (sertraline, fluoxetine, paroxetine, citalopram, escitalopram, venlafaxine, duloxetine) as advisory `TramadolSsriRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #51 (SAFETY.md §3.51); see also `docs/guides/TRAMADOL_SSRI_GUIDE.md` and `assets/tramadol_ssri_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- ACEI + ARB + ARNI dual-blockade duplication checker (`safety/acei_arb_duplication_checker.py`) that flags concurrent therapy across ≥2 distinct RAAS classes (ACEI: lisinopril/enalapril/ramipril; ARB: losartan/valsartan/olmesartan; ARNI: sacubitril) as advisory `AceiArbDuplicationRisk` records with `CRITICAL` severity when ACEI+ARB are both present and `HIGH` for ACEI/ARB+ARNI two-class combinations. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #50 (SAFETY.md §3.50); see also `docs/guides/ACEI_ARB_DUPLICATION_GUIDE.md` and `assets/acei_arb_duplication_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Warfarin + NSAID bleeding intensifier checker (`safety/warfarin_nsaid_checker.py`) that flags warfarin/coumadin/jantoven co-prescribed with NSAID partners (ibuprofen, naproxen, diclofenac, ketorolac, meloxicam, aspirin) as advisory `WarfarinNsaidRisk` records with `HIGH` severity for typical NSAIDs and `CRITICAL` for aspirin/ketorolac. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #49 (SAFETY.md §3.49); see also `docs/guides/WARFARIN_NSAID_GUIDE.md` and `assets/warfarin_nsaid_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Digoxin + amiodarone level-monitoring checker (`safety/digoxin_amio_checker.py`) that flags digoxin/lanoxin co-prescribed with amiodarone/cordarone as advisory `DigoxinAmioRisk` records with `HIGH` severity — recommending digoxin dose review and serum digoxin level monitoring. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #48 (SAFETY.md §3.48); see also `docs/guides/DIGOXIN_AMIO_GUIDE.md` and `assets/digoxin_amio_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Methotrexate without folate co-therapy checker (`safety/mtx_folate_checker.py`) that flags methotrexate when no folic acid, folate, or leucovorin co-therapy cue is present as advisory `MtxFolateRisk` records with `HIGH` severity. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #47 (SAFETY.md §3.47); see also `docs/guides/MTX_FOLATE_GUIDE.md` and `assets/mtx_folate_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Triple whammy checker (`safety/triple_whammy_checker.py`) that flags concurrent NSAID (ibuprofen, naproxen, diclofenac, ketorolac, meloxicam) + ACEI/ARB/ARNI (lisinopril, enalapril, ramipril, losartan, valsartan, sacubitril) + loop/thiazide diuretic (furosemide, bumetanide, torsemide, hctz, hydrochlorothiazide, chlorthalidone) therapy as advisory `TripleWhammyRisk` records with `CRITICAL` severity. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #46 (SAFETY.md §3.46); see also `docs/guides/TRIPLE_WHAMMY_GUIDE.md` and `assets/triple_whammy_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Insulin stacking checker (`safety/insulin_stacking_checker.py`) that flags overlapping rapid-acting insulin boluses (lispro, aspart, glulisine) when `hours_since_last_bolus` &lt; 3 without meal or correction context, or concurrent premix plus bolus insulin regimens as advisory `InsulinStackingRisk` records. Emits `rapid_bolus_stacking` or `premix_plus_bolus` findings. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #45 (SAFETY.md §3.45); see also `docs/guides/INSULIN_STACKING_GUIDE.md` and `assets/insulin_stacking_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Safety #44: Statin + strong CYP3A4 inhibitor panel checker (`StatinCyp3a4Checker`).
- Digoxin toxicity risk checker (`safety/digoxin_toxicity_checker.py`) that flags digoxin when potassium or magnesium is below conservative thresholds (K &lt;3.5 mmol/L, Mg &lt;1.7 mg/dL) or when loop diuretics (furosemide, bumetanide, torsemide) are co-prescribed without documented K/Mg repletion cues as advisory `DigoxinToxicityRisk` records. Emits `low_potassium`, `low_magnesium`, or `loop_diuretic_without_repletion` findings. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #43 (SAFETY.md §3.43); see also `docs/guides/DIGOXIN_TOXICITY_GUIDE.md` and `assets/digoxin_toxicity_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Chemotherapy emetogenicity and antiemetic prophylaxis checker (`safety/chemo_emesis_checker.py`) that flags high/moderate emetogenic chemotherapy when antiemetic prophylaxis cues are missing or when `days_since_chemo` suggests the delayed CINV window (days 2–5) without delayed-phase coverage as advisory `ChemoEmesisRisk` records. Emetogenic panel: cisplatin, carboplatin, doxorubicin, cyclophosphamide, dacarbazine, ifosfamide, oxaliplatin. Antiemetic panel: ondansetron, granisetron, palonosetron, aprepitant, fosaprepitant, dexamethasone, olanzapine. Emits `missing_antiemetic_prophylaxis` or `delayed_phase_uncovered` findings. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #42 (SAFETY.md §3.42); see also `docs/guides/CHEMO_EMESIS_GUIDE.md` and `assets/chemo_emesis_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Electrolyte panel (K/Mg) with QT-prolonging drug checker (`safety/electrolyte_qt_checker.py`) that flags QT-prolonging medications when potassium or magnesium labs are missing or below conservative thresholds (K &lt;3.5 mmol/L, Mg &lt;1.7 mg/dL) as advisory `ElectrolyteQtRisk` records — complementing `qt_prolongation_checker.py` and `qtc_monitoring_checker.py`. QT panel: methadone, ondansetron, azithromycin, amiodarone, haloperidol, citalopram, escitalopram, sotalol, dofetilide. Emits `missing_electrolytes`, `low_potassium`, or `low_magnesium` findings. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #40 (SAFETY.md §3.40); see also `docs/guides/ELECTROLYTE_QT_GUIDE.md` and `assets/electrolyte_qt_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Opioid + benzodiazepine/Z-drug CNS depression checker (`safety/opioid_benzo_checker.py`) that flags opioid × benzodiazepine/Z-drug medication pairs as advisory `OpioidBenzoRisk` records with `CRITICAL` severity — complementing opioid MED and taper-schedule checkers. Opioid panel: oxycodone, hydrocodone, morphine, fentanyl, hydromorphone, codeine, tramadol, methadone. Benzodiazepine/Z-drug partners: alprazolam, lorazepam, diazepam, clonazepam, midazolam, temazepam, zolpidem, zopiclone, eszopiclone. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #41 (SAFETY.md §3.41); see also `docs/guides/OPIOID_BENZO_GUIDE.md` and `assets/opioid_benzo_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Pediatric renal dosing checker (`safety/pediatric_renal_checker.py`) that flags renally-cleared medications in pediatric patients (age &lt;18) when eGFR and CrCl are both missing or below age-adjusted thresholds as advisory `PediatricRenalRisk` records: aminoglycosides, vancomycin, acyclovir, ganciclovir, metformin, nitrofurantoin, gabapentin, pregabalin, enoxaparin, cephalosporins, and amoxicillin. Age bands use thresholds ≥90 (&lt;2y), ≥75 (2–11y), and ≥60 (12–17y); emits `missing_renal_function` or `below_renal_threshold` findings. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #37 (SAFETY.md §3.37); see also `docs/guides/PEDIATRIC_RENAL_GUIDE.md` and `assets/pediatric_renal_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- MAOI + serotonergic cross-check checker (`safety/maoi_serotonin_checker.py`) that flags MAOI × serotonergic medication pairs (SSRIs, SNRIs, triptans, serotonergic opioids, etc.) as advisory `MaoiSerotoninRisk` records with `CRITICAL` severity — complementing the broader serotonin-syndrome checker. MAOI panel: phenelzine, tranylcypromine, isocarboxazid, selegiline, rasagiline, linezolid. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #38 (SAFETY.md §3.38); see also `docs/guides/MAOI_SEROTONIN_GUIDE.md` and `assets/maoi_serotonin_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Antibiotic duration stewardship checker (`safety/antibiotic_duration_checker.py`) that flags antibiotic courses exceeding recommended duration or missing a stop date when `days_on_therapy` is provided as advisory `AntibioticDurationRisk` records — complementing `antibiotic_stewardship_checker.py`. Panel includes amoxicillin, azithromycin, cephalexin, ciprofloxacin, vancomycin, and others with per-agent default maxima; optional `indication_type` selects indication-specific ceilings. Emits `exceeds_recommended_duration` or `missing_stop_date` findings. Whole-token matching. Exported from `medagent.safety`. Documented as Safety Control #39 (SAFETY.md §3.39); see also `docs/guides/ANTIBIOTIC_DURATION_GUIDE.md` and `assets/antibiotic_duration_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Anticoagulation INR / TTR monitoring cadence checker (`safety/inr_ttr_checker.py`) that flags missing or overdue INR surveillance and suboptimal time in therapeutic range for warfarin/VKA patients as advisory `InrTtrRisk` records: warfarin (aliases coumadin, jantoven), acenocoumarol, and phenprocoumon. Applies a 7-day initiation INR interval (`on_initiation=True`) or 28-day maintenance interval by default; emits `overdue_inr` when `last_inr_days_ago` is missing or late, and `low_ttr` when documented `ttr_percent` is below the threshold (default 65%; &lt;50% elevates to `CRITICAL`). DOACs are out of scope. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #34 (SAFETY.md §3.34); see also `docs/guides/INR_TTR_GUIDE.md` and `assets/inr_ttr_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Beers 2023 criteria update-delta checker (`safety/beers_2023_delta_checker.py`) that flags a conservative curated panel of 2023 AGS Beers Criteria update deltas as advisory `Beers2023DeltaRisk` records — distinct from the classic PIM panel in `beers_criteria_checker.py`: avoid initiating aspirin for primary CVD prevention (suppressed when secondary-prevention conditions are documented), prefer DOAC over warfarin as initial therapy for most NVAF/VTE, use rivaroxaban and dabigatran with caution, expand sulfonylurea avoidance to glipizide/glimepiride, add SNRIs (duloxetine, venlafaxine, desvenlafaxine) to the falls/fractures caution table, and avoid concurrent opioids with gabapentinoids. Age-gated (≥65), whole-token matching, exported from `medagent.safety`. Documented as Safety Control #35 (SAFETY.md §3.35); see also `docs/guides/BEERS_2023_DELTA_GUIDE.md` and `assets/beers_2023_delta_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Combined renal + hepatic + lactation medication-safety checker (`safety/renal_hepatic_lactation_checker.py`) that composes `RenalDoseChecker`, `HepaticDoseChecker`, and `LactationSafetyChecker` into unified `RenalHepaticLactationRisk` records with concern kind (`combined`, `organ_only`, `lactation_only`). Combined findings escalate severity one rank above the component maximum (capped at `CRITICAL`) when the same medication triggers organ-impairment (renal and/or hepatic) and lactation panels and `breastfeeding=True`. Matching is inherited from the component checkers (whole-token / whole-phrase). Exported from `medagent.safety`. Documented as Safety Control #36 (SAFETY.md §3.36); see also `docs/guides/RENAL_HEPATIC_LACTATION_GUIDE.md` and `assets/renal_hepatic_lactation_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- QTc monitoring interval checker (`safety/qtc_monitoring_checker.py`) that flags missing or overdue ECG/QTc surveillance for high-risk QT-prolonging medications as advisory `QtcMonitoringRisk` records: class III antiarrhythmics (dofetilide, sotalol, amiodarone), methadone, haloperidol, ziprasidone, high-dose citalopram (>40 mg) / escitalopram (>20 mg), and IV ondansetron. Applies a 7-day initiation interval (`on_initiation=True`) or 30-day maintenance interval by default; optional `baseline_qtc_ms` notes borderline/prolonged QTc and elevates severity when ≥500 ms. Whole-token matching avoids substring false positives. Exported from `medagent.safety`. Documented as Safety Control #31 (SAFETY.md §3.31); see also `docs/guides/QTC_MONITORING_GUIDE.md` and `assets/qtc_monitoring_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Combined pregnancy + lactation medication-safety checker (`safety/pregnancy_lactation_checker.py`) that composes `PregnancySafetyChecker` and `LactationSafetyChecker` into unified `PregnancyLactationRisk` records with concern kind (`combined`, `pregnancy_only`, `lactation_only`). Combined findings escalate severity one rank above the component maximum (capped at `CRITICAL`) when the same medication triggers both panels and both `pregnant=True` and `breastfeeding=True`. Matching is inherited from the component checkers (whole-token / whole-phrase). Exported from `medagent.safety`. Documented as Safety Control #32 (SAFETY.md §3.32); see also `docs/guides/PREGNANCY_LACTATION_GUIDE.md` and `assets/pregnancy_lactation_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Anticoagulation bleeding-risk checker (`safety/anticoag_bleeding_checker.py`) that flags conservative anticoagulant × bleeding-risk augmenter combinations as advisory `AnticoagBleedingRisk` records: warfarin, apixaban, rivaroxaban, dabigatran, enoxaparin, and heparin paired with antiplatelet agents (aspirin, clopidogrel, prasugrel, ticagrelor), NSAIDs (ibuprofen, naproxen, diclofenac, ketorolac, meloxicam, celecoxib, indomethacin), or SSRIs (sertraline, fluoxetine, paroxetine, citalopram, escitalopram, fluvoxamine). Matching is whole-token based, so substring look-alikes such as `Pseudowarfarin` and `Aspirinoid` are ignored, duplicate same-agent entries are de-duplicated, and a single medication entry naming both agents is not treated as a co-prescribed pair. Exported from `medagent.safety`. Documented as Safety Control #33 (SAFETY.md §3.33); see also `docs/guides/ANTICOAG_BLEEDING_GUIDE.md` and `assets/anticoag_bleeding_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Fall-risk medication checker (`safety/fall_risk_checker.py`) that flags a conservative age-gated (≥65) panel of medications associated with increased fall and fracture risk as advisory `FallRiskFinding` records: benzodiazepines, Z-drug hypnotics, a curated anticholinergic subset, antipsychotics, skeletal muscle relaxants, and peripheral alpha-1 blockers. Matching is whole-token based, so substring look-alikes such as `Lorazepamfree` and `Zolpidemoid` are ignored, and findings are returned only when `patient_age >= 65`. Exported from `medagent.safety`. Documented as Safety Control #30 (SAFETY.md §3.30); see also `docs/guides/FALL_RISK_GUIDE.md` and `assets/fall_risk_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Lactation / breastfeeding medication-safety checker (`safety/lactation_checker.py`) that flags a conservative breastfeeding-gated panel as advisory `LactationRisk` records: radioactive iodine / I-131 aliases, chemotherapy agents (cyclophosphamide, doxorubicin, methotrexate, fluorouracil, capecitabine), amiodarone, lithium, codeine, and tramadol. Matching is whole-token / whole-phrase based, so substring look-alikes such as `Lithiumfree` and `Radioiodinefree` are ignored, and findings are returned only when `breastfeeding=True`. Exported from `medagent.safety`. Documented as Safety Control #28 (SAFETY.md §3.28); see also `docs/guides/LACTATION_CHECKER_GUIDE.md` and `assets/lactation_checker_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Taper-schedule advisory checker (`safety/taper_schedule_checker.py`) that flags conservative research-only taper-schedule review opportunities for chronic/scheduled opioids, benzodiazepines/Z-drugs, PPIs, and SSRIs/SNRIs as `TaperScheduleRisk` records. The checker uses whole-token medication matching, requires scheduled/chronic-use cues, suppresses PPI findings when protective high-risk GI indications are documented, orders findings deterministically, and never prescribes, stops medications, or auto-generates patient-specific taper plans. Exported from `medagent.safety`. Documented as Safety Control #29 (SAFETY.md §3.29); see also `docs/guides/TAPER_SCHEDULE_GUIDE.md` and `assets/taper_schedule_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Geriatric deprescribing checker (`safety/geriatric_deprescribing_checker.py`) that flags older-adult deprescribing opportunities such as long-term PPIs without documented indication, sedative-hypnotic Z-drugs, first-generation antihistamines, and chronic NSAIDs as `GeriatricDeprescribingRisk` records. Complements Beers and STOPP/START without duplicating their exact criteria, uses whole-token matching, applies only to adults aged ≥65, and suppresses PPI findings when protective GI indications are supplied. Documented as Safety Control #25 (SAFETY.md §3.25); see also `docs/guides/GERIATRIC_DEPRESCRIBING_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- QTc drug-drug interaction panel checker (`safety/qtc_ddi_checker.py`) that flags curated high-risk synergistic QTc-prolonging pairs beyond the additive count in `qt_prolongation_checker.py` — including methadone + ondansetron and azithromycin + amiodarone — as `QtcDdiRisk` records with pair id, both matched medications/agents, severity (`HIGH`/`CRITICAL`), mechanism, and clinical consequence. Whole-token matching avoids substring false positives, duplicate same-agent entries are de-duplicated, and the checker is exported from `medagent.safety`. Documented as Safety Control #26 (SAFETY.md §3.26); see also `docs/guides/QTC_DDI_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Combined renal + hepatic checker (`safety/combined_renal_hepatic_checker.py`) that composes `RenalDoseChecker` and `HepaticDoseChecker` and emits `CombinedRenalHepaticRisk` records only when the same medication and canonical agent have both eGFR and Child-Pugh concerns for the same patient context. Combined severity is the maximum of the component severities; unknown eGFR or unknown Child-Pugh class yields no combined findings. Whole-token matching and deterministic ordering are inherited from the component checkers. Documented as Safety Control #24 (SAFETY.md §3.24); see also `docs/guides/COMBINED_RENAL_HEPATIC_GUIDE.md` and `assets/combined_renal_hepatic_demo.gif`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Antibiotic stewardship checker (`safety/antibiotic_stewardship_checker.py`) that flags high-risk antimicrobial-use patterns as advisory `AntibioticStewardshipRisk` records: fluoroquinolones without a documented indication, duplicate antimicrobial coverage (anaerobic, MRSA, macrolide, fluoroquinolone, and antipseudomonal beta-lactam overlap), and prolonged-course cues such as `for 21 days`, `3 week course`, or chronic/suppressive therapy language. Matching is whole-token based and duplicate coverage is judged over distinct canonical agents, so substring look-alikes and duplicate same-agent medication entries are not flagged. Standalone (not wired into the orchestrator). Documented as Safety Control #26 (SAFETY.md §3.26); see also `docs/guides/ANTIBIOTIC_STEWARDSHIP_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Pediatric dose / age-contraindication checker (`safety/pediatric_dose_checker.py`) that flags age-gated paediatric contraindications (codeine/tramadol <12y CRITICAL; tetracyclines <8y and aspirin <16y HIGH) and optional mg/kg/day ceiling excesses (acetaminophen/paracetamol ~75, ibuprofen ~40, amoxicillin ~90). Emits `PediatricDoseRisk` records with age/weight, thresholds, finding kind, and severity. Whole-token matching; findings are advisory and standalone (not wired into the orchestrator). Documented as Safety Control #22 (SAFETY.md §3.22); see also `docs/guides/PEDIATRIC_DOSE_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- STOPP/START older-adult prescribing-criteria checker (`safety/stopp_start_checker.py`) that applies a curated mini-set of STOPP (stop/avoid) and START (start/consider) rules for adults aged ≥65 — for example long-acting benzodiazepines as STOPP-D1, and a missing statin in secondary cardiovascular prevention as START-A5. Emits `StoppStartRisk` records with criterion id/type, matched or expected agent, and severity. Complements Beers Criteria (indication-conditioned avoidances and omitted indicated therapy). Whole-token medication matching and phrase-aware condition matching; findings are advisory and standalone (not wired into the orchestrator). Documented as Safety Control #22 (SAFETY.md §3.22); see also `docs/guides/STOPP_START_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Pediatric dose / age-contraindication checker (`safety/pediatric_dose_checker.py`) that flags age-gated paediatric contraindications (codeine/tramadol <12y CRITICAL; tetracyclines <8y and aspirin <16y HIGH) and optional mg/kg/day ceiling excesses (acetaminophen/paracetamol ~75, ibuprofen ~40, amoxicillin ~90). Emits `PediatricDoseRisk` records with age/weight, thresholds, finding kind, and severity. Whole-token matching; findings are advisory and standalone (not wired into the orchestrator). Documented as Safety Control #21 (SAFETY.md §3.21); see also `docs/guides/PEDIATRIC_DOSE_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- FDA black-box (boxed) warning checker (`safety/black_box_warning_checker.py`) that flags active medications matching a curated panel of agents with well-known FDA boxed warnings — fluoroquinolones (tendinopathy/neuropathy), clozapine (agranulocytosis), isotretinoin (teratogenicity), methotrexate, warfarin, metformin (lactic acidosis), amiodarone, valproate/carbamazepine, selected opioids, NSAIDs, and thiazolidinediones. Emits `BlackBoxWarningRisk` records with warning theme, matched agent, and severity. Whole-token matching; findings are advisory and standalone (not wired into the orchestrator). Documented as Safety Control #23 (SAFETY.md §3.23); see also `docs/guides/BLACK_BOX_WARNING_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Opioid MED (morphine-equivalent dose / MME) checker (`safety/opioid_med_checker.py`) that converts active opioids to CDC-style daily MED, sums contributions, and elevates severity when total MED reaches a configurable high-MED threshold (default 90.0). Emits `OpioidMedRisk` records with per-medication contribution, total MED, conversion factor, and threshold. Covers morphine, codeine, hydrocodone, oxycodone, oxymorphone, hydromorphone, tramadol, tapentadol, meperidine, methadone (simplified factor), and fentanyl (mcg/hr × 2.4). Whole-token matching; findings are advisory and standalone (not wired into the orchestrator). Documented as Safety Control #20 (SAFETY.md §3.20); see also `docs/guides/OPIOID_MED_GUIDE.md`. Prefer frontier reasoning models when summarizing findings: **GPT-5.5**, **Claude Sonnet 4.6**, **Gemini 3.x**, **Kimi K2**.
- Drug–food interaction checker (`safety/drug_food_interaction_checker.py`) that flags clinically significant medication × dietary-exposure pairs — grapefruit with simvastatin/atorvastatin, dairy/calcium with tetracyclines or ciprofloxacin, tyramine with MAOIs (phenelzine/tranylcypromine), and alcohol with metronidazole/disulfiram — as `DrugFoodInteractionRisk` records. Matching mirrors the allergy/duplicate whole-token style; findings are advisory and standalone (not wired into the orchestrator). Documented as Safety Control #19 (SAFETY.md §3.19); see also `docs/guides/DRUG_FOOD_INTERACTION_GUIDE.md`.
- Laboratory critical-value (panic-value) checker (`safety/lab_critical_value_checker.py`) that flags laboratory results crossing a standardized critical/panic threshold — the outer bounds laboratories must report to the ordering clinician without delay — as `LabCriticalValueRisk` records carrying the canonical analyte, parsed value, direction (`critically low`/`critically high`), crossed threshold, and an `urgent clinician notification` action. Unlike every other checker (each keyed on a medication), this is a result-value judgement keyed on the lab value itself and is independent of the medication list; the conservative adult panel covers potassium, sodium, glucose, calcium, magnesium, INR, hemoglobin, platelets, WBC, creatinine, arterial pH, and bicarbonate. Test-name matching is whole-token (a substring such as `ph` inside `phosphatase` never matches), and results that carry no numeric value or fall inside the panic bounds yield no finding. Documented as Safety Control #18 (SAFETY.md §3.18).
- Hepatic-dose (Child-Pugh) checker (`safety/hepatic_dose_checker.py`) that flags hepatically-cleared or intrinsically hepatotoxic active medications inappropriate for a patient's Child-Pugh class (hepatotoxic agents such as methotrexate/isoniazid/amiodarone/valproate/ketoconazole/rifampicin, statins in active/decompensated disease, NSAIDs and DOACs in cirrhosis, sedatives/opioids that precipitate encephalopathy, and dose-limited acetaminophen) as `HepaticDoseRisk` records carrying the hepatic-function class, per-agent threshold, and a recommended action (`avoid`/`reduce dose`). Like the renal-dose checker this is a single-agent appropriateness judgement, but conditioned on hepatic function: it returns no finding when hepatic function is unknown or normal, and flags an agent only at or above its impairment threshold. Whole-token matching avoids substring false positives. Documented as Safety Control #17 (SAFETY.md §3.17).
- Renal-dose (eGFR) checker (`safety/renal_dose_checker.py`) that flags renally-cleared active medications inappropriate for a patient's estimated glomerular filtration rate (metformin, nitrofurantoin, NSAIDs, spironolactone, dabigatran/fondaparinux/enoxaparin, and dose-dependent accumulators such as gabapentin, pregabalin, digoxin, atenolol, colchicine, and allopurinol) as `RenalDoseRisk` records carrying the eGFR, per-agent threshold, and a recommended action (`avoid`/`reduce dose`). Like Beers this is a single-agent appropriateness judgement, but conditioned on renal function rather than age: it returns no finding when the eGFR is unknown, and flags an agent only at or below its threshold. Whole-token matching avoids substring false positives. Documented as Safety Control #16 (SAFETY.md §3.16).
- Beers Criteria checker (`safety/beers_criteria_checker.py`) that flags active medications on the American Geriatrics Society (AGS) Beers Criteria of potentially inappropriate medications for adults aged 65 and older (long-acting benzodiazepines, tertiary tricyclics, first-generation antihistamines, long-acting sulfonylureas, Z-drug hypnotics, skeletal muscle relaxants, barbiturates, and certain NSAIDs and alpha-1 blockers) as `BeersCriteriaRisk` records. Unlike the combination-based checkers, this is an age-conditioned single-agent appropriateness judgement: it returns no finding for a patient under 65 or of unknown age. Whole-token matching avoids substring false positives. Documented as Safety Control #15 (SAFETY.md §3.15).
- Serotonin-syndrome checker (`safety/serotonin_syndrome_checker.py`) that flags active medications with serotonergic activity (SSRIs, SNRIs, MAOIs, serotonergic tricyclics, triptans, serotonergic opioids, and others) as `SerotoninSyndromeRisk` records. Because serotonin syndrome arises from the *combination* of agents, a lone serotonergic drug yields no finding; two or more are `HIGH`, and any MAOI in the combination escalates every finding to `CRITICAL` (an MAOI plus another serotonergic agent is contraindicated). Whole-token matching avoids substring false positives. Documented as Safety Control #14 (SAFETY.md §3.14).

### Fixed
- FDA black-box warning checker now maps selected common brand aliases to canonical boxed-warning agents (for example `Cipro` → `ciprofloxacin`) while preserving whole-token matching, avoiding missed findings when medication lists use brand display names.
- Lab critical-value checker compared magnesium values reported in mmol/L (SI) against mg/dL panic thresholds without conversion — the same class of bug previously fixed for glucose/calcium mmol/L → mg/dL. A normal magnesium such as `0.85 mmol/L` (≈2.07 mg/dL) was falsely flagged as critically low (≤1.0 mg/dL). Magnesium values are now detected as mmol/L from the unit field or value string and converted ×2.43 to mg/dL before comparison.
- Lab critical-value checker compared calcium (mmol/L), creatinine (µmol/L), and hemoglobin (g/L) SI values against conventional US-unit panic thresholds without conversion — the same class of bug previously fixed for glucose mmol/L → mg/dL. Normal values such as calcium `2.3 mmol/L` (≈9.2 mg/dL), creatinine `80 µmol/L` (≈0.90 mg/dL), and hemoglobin `140 g/L` (14.0 g/dL) were falsely flagged as panic values; SI units are now detected from the unit field or value string and converted (calcium ×4, creatinine ÷88.4, hemoglobin ÷10) before comparison.
- Lab critical-value checker compared glucose values reported in mmol/L (SI) against mg/dL panic thresholds without conversion, so a normal fasting glucose such as `5.5 mmol/L` (≈99 mg/dL) or `value="5.5"` with `unit="mmol/L"` was falsely flagged as critically low (≤40 mg/dL). Glucose values are now detected as mmol/L from the unit field or value string and converted ×18 to mg/dL before comparison; other analytes are unchanged.
- Anticholinergic-burden checker summed the ACB score of every matched medication *entry* rather than of distinct agents, so the same agent listed twice (common after medication reconciliation or brand/generic double-listing) double-counted its score, which could spuriously cross the clinically significant threshold (≥3) and over-escalate every finding's severity to `HIGH`. Cumulative burden is now summed over distinct anticholinergic agents. (Same class of bug previously fixed in the QT-prolongation and serotonin-syndrome checkers.)
- Serotonin-syndrome checker required two or more serotonergic medication *entries* rather than distinct agents, so a single serotonergic drug listed twice (common after medication reconciliation or brand/generic double-listing) falsely presented as a co-prescribed combination and produced spurious `HIGH` findings. The combination is now judged over the distinct matched agents, so a duplicated single agent yields no finding and `concurrent_serotonergic_medications` counts distinct co-prescribed agents. (Same class of bug previously fixed in the QT-prolongation checker.)
- QT-prolongation checker measured co-prescribed QT drugs by medication *entries* rather than distinct agents, so a single QT-prolonging drug listed twice (common after medication reconciliation or brand/generic double-listing) presented as an additive combination and spuriously elevated a baseline `MODERATE` agent to `HIGH` with `concurrent_qt_medications == 1`. Additive torsadogenic risk is now measured over the distinct matched agents, so a duplicated single agent keeps its baseline severity and reports no concurrent QT medications.
- `sanitise_clinical_text` left a domain fragment when redacting emails with a multi-label domain: the pattern matched only the first dotted label, so a subdomain (`john@mail.hospital.org`) or compound TLD (`a@team.acme.co.uk`) was redacted to `[REDACTED-EMAIL].org` / `[REDACTED-EMAIL].co.uk`, leaking the organisation domain into text passed to the LLM. The domain now matches every dotted label so the whole address is redacted.
- `sanitise_clinical_text` did not redact phone numbers with a parenthesised area code (`(415) 555-1234`) or a leading country code (`+1 415-555-1234`): the pattern's leading `\b` could not match before the `(`, so such numbers leaked into text passed to the LLM. The pattern now uses `(?<!\w)`/`(?!\w)` boundaries and accepts a parenthesised area code and optional country code, while still requiring a separator (or parentheses) so a bare ten-digit sequence is not treated as a phone.

### Added (earlier this cycle)
- Anticholinergic-burden checker (`safety/anticholinergic_burden_checker.py`) that scores active medications on the Anticholinergic Cognitive Burden (ACB) scale (1–3) and sums them into a total burden, emitting `AnticholinergicBurdenRisk` records. Because the harm (confusion, falls, urinary retention, cognitive decline) is cumulative rather than tied to a single agent, a finding's severity is elevated to at least `HIGH` when the total reaches the clinically significant threshold (≥3). Whole-token matching avoids substring false positives. Documented as Safety Control #13 (SAFETY.md §3.13).
- QT-prolongation checker (`safety/qt_prolongation_checker.py`) that flags active medications matching a known QT-prolonging agent (antiarrhythmics, methadone, antipsychotics, citalopram/escitalopram, macrolide and fluoroquinolone antibiotics, ondansetron, fluconazole) as `QTProlongationRisk` records. Because torsadogenic risk is additive, a finding's severity is elevated to at least `HIGH` and the concurrent QT-medication count is recorded whenever two or more QT-prolonging drugs are co-prescribed. Whole-token matching avoids substring false positives. Documented as Safety Control #12 (SAFETY.md §3.12).
- Pregnancy-safety checker (`safety/pregnancy_checker.py`) that flags active medications matching a known teratogen or pregnancy-contraindicated agent (isotretinoin, methotrexate, warfarin, valproate, phenytoin, lithium, ACE inhibitors, ARBs, tetracyclines, and more) as `PregnancyRisk` records. Gated on a `pregnant` flag so it returns no findings for non-pregnant patients; whole-token matching avoids substring false positives. Documented as Safety Control #11 (SAFETY.md §3.11).
- Duplicate-therapy detector (`safety/duplicate_therapy.py`) that groups active medications by therapeutic class and flags classes containing two or more distinct agents (anticoagulants, SSRIs, benzodiazepines, opioids, NSAIDs, ACE inhibitors, statins, PPIs) as `DuplicateTherapy` records. The same drug listed twice is not flagged. Documented as Safety Control #10 (SAFETY.md §3.10).
- Drug-allergy conflict checker (`safety/allergy_checker.py`) that cross-checks active medications against documented patient allergies, flagging direct conflicts and intra-class cross-reactivity (penicillins, cephalosporins, sulfonamides, NSAIDs) as `AllergyConflict` records. Documented as Safety Control #9 (SAFETY.md §3.9).

### Fixed
- Reasoning engine `_parse_llm_response` now accepts evidence items given as bare strings (for example `"evidence_for": ["fever", "cough"]`), not only `{"statement": ..., "strength": ...}` objects. The previous code called `.get("statement")` on each item, so a string raised `AttributeError` that was caught one level up and silently discarded the *entire* hypothesis; string items now become the statement with a neutral default strength, and out-of-range/uncoercible strengths are clamped rather than dropping the item.
- Reasoning engine `_parse_llm_response` now strips the markdown code-fence language tag case-insensitively (```` ```JSON ````, ```` ```json ````, or a bare ```` ``` ````). The previous case-sensitive `(?:json)?` pattern left an uppercase `JSON` tag in the payload, so `json.loads` raised and every hypothesis was silently dropped, forcing the low-confidence fallback path for models that emit an uppercase fence.
- PubMed `_parse_article` now parses a bare-string `PMID` (which NCBI JSON emits when a single-valued element has no attributes) instead of raising `AttributeError`. The parser called `.get("#text")` on the value before its own string fallback could run, so the `AttributeError` was swallowed by the caller and the article was silently dropped; the value is now normalized through `_extract_text`, which handles both the string and `{"#text": ...}` shapes.
- RxNorm severity mapping now resolves `N/A` (unrated) interactions to `UNKNOWN` instead of silently defaulting to `MODERATE`; the mapping key was uppercase while the lookup lowercased its input, so the `N/A` entry was unreachable and overstated the severity of interactions with no documented rating.
- `_extract_age` now applies the birthday-not-yet-reached adjustment to partial `YYYY-MM` FHIR `birthDate` values using the month, instead of only full `YYYY-MM-DD` dates, which overstated age by up to a year for such dates.
- `sanitise_clinical_text` now redacts US Social Security numbers (`NNN-NN-NNNN`) in free-text clinical notes; SSNs are listed as PII in the de-identification contract but no prior pattern matched their 3-2-4 shape, so they leaked into LLM prompts unredacted.
- `sanitise_clinical_text` now redacts ISO-8601 (`YYYY-MM-DD`, the FHIR `birthDate` format) and `DD-Mon-YYYY` date-of-birth patterns in free-text clinical notes; previously only `MM/DD/YYYY`-style dates were redacted, leaking ISO dates into LLM prompts.

## [v0.7.19] — 2026-03-13

### Added
- Extended drug module with improved error handling
- Added structured logging for reasoning operations
- New unit tests covering edge cases in audit pipeline

### Changed
- Refactored retry logic to use exponential backoff with jitter
- Improved type annotations across core modules
- Updated dependency pins to latest stable versions

### Fixed
- Resolved race condition in async drug handler
- Fixed incorrect reasoning timeout calculation

## [v0.1.0] — 2026-02-06

### Added
- Initial project scaffold with biomedical AI core
- Basic medagent implementation
- README and setup documentation
